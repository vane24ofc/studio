import { ComingSoon } from '@/components/shared/ComingSoon';

export default function AdminLMSCoursesPage() {
  return (
    <ComingSoon 
      pageTitle="Gestión de Cursos"
      description="Crea, edita y organiza los cursos de capacitación, define módulos, lecciones y gestiona participantes."
    />
  );
}
