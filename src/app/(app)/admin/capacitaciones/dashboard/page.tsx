import { ComingSoon } from '@/components/shared/ComingSoon';

export default function AdminLMSDashboardPage() {
  return (
    <ComingSoon 
      pageTitle="Dashboard de Capacitaciones (LMS)"
      description="Visualiza estadísticas generales de los cursos, inscripciones, progreso y más."
    />
  );
}
